export function makeStatsString(headCount, middleCount, bottomCount) {
    return `You clicked on the head 4 times, on the middle 5 times, and on the bottom 1 time. And everybody loves your character's catchphrases:`;
}